import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    btn: {
        backgroundColor: '#ff8e00',
        borderRadius: 10,
        marginBottom: 20,
        marginHorizontal: 20
    },
})