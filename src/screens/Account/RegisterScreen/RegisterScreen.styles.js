import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
    logo: {
        resizeMode: 'contain',
        width: "100%",
        height: 200,
    },
    containerLogo: {
        marginTop: 20
    },
    contentform: {
        marginHorizontal: 30
    }
})