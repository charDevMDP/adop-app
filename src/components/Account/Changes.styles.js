import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
    view: {
        alignItems: 'center',
        paddingBottom: 10
    },
    btn: {
        backgroundColor: '#ff8e00',
        borderRadius: 10
    },
})
